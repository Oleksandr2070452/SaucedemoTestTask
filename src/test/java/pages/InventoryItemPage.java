package pages;

import elements.InventoryItemElements;
import org.openqa.selenium.WebDriver;

public class InventoryItemPage extends InventoryItemElements {
    public InventoryItemPage(WebDriver driver) {
        super(driver);
    }
}
